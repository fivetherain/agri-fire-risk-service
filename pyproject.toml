[project]
name = "agri-fire-risk-service"
version = "0.1.0"
description = ""
authors = [
    {name = "Zach",email = "471288717@qq.com"}
]
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "fastapi (>=0.133.1,<0.134.0)",
    "uvicorn (>=0.41.0,<0.42.0)",
    "sqlalchemy (>=2.0.47,<3.0.0)",
    "psycopg2-binary (>=2.9.11,<3.0.0)",
    "geoalchemy2 (>=0.18.3,<0.19.0)",
    "python-dotenv (>=1.2.1,<2.0.0)",
    "shapely (>=2.1.2,<3.0.0)"
]


[tool.poetry]
package-mode = false

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"

[dependency-groups]
dev = [
    "pytest (>=9.0.2,<10.0.0)",
    "ruff (>=0.15.4,<0.16.0)",
    "alembic (>=1.18.4,<2.0.0)",
    "httpx (>=0.28.1,<0.29.0)"
]
[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]